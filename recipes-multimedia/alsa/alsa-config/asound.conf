# Bit-Perfect ALSA Configuration for Auris Audio
# Direct hardware access for 44.1-192kHz playback

# Default PCM - Direct hardware output
pcm.!default {
    type hw
    card 0
    device 0
}

# Default mixer control
ctl.!default {
    type hw
    card 0
}
